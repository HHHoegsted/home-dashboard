# Home Dashboard

Tablet-first family dashboard with a React frontend and a FastAPI backend.

## Services

- `tablet-site` — frontend
- `tablet-site-backend` — backend API

## Run with Docker Compose

From this folder:

```bash
docker compose up --build